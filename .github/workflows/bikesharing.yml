name: Master

on: [push]
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master

    - uses: Azure/k8s-bake@users/shigupt/addingHelmDependencyUpdate
      with:
          renderEngine: 'helm2'
          helmChart: './samples/BikeSharingApp/charts'
          helm-version: 'latest'                                                   
                                
                
                            
      id: bake
